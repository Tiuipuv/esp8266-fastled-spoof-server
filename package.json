{
  "name": "esp8266-spoof-server",
  "version": "1.0.0",
  "description": "A server for simulating the ESP8266's API for testing and development purposes.",
  "main": "server/main.js",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "nodemon server/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jordan Jensen",
  "license": "MIT",
  "nodemonConfig": {
    "watch": [
      "server/**/*"
    ]
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.11"
  }
}
